//iterable 
// iterable is a collection of items, such as a list or a tuple.  on which we can apply for of loop
// we can use for of loop on iterable data types like array, set, map, string etc
const numbers = [1, 2, 3, 4, 5];
for (const num of numbers) {
    console.log(num);
    }
    const name ="manish"
    for (const letter of name) {
        console.log(letter);
        }
        